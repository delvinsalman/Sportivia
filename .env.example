# Optional override. Leave unset for same-origin /duel (recommended for live deploy).
# Local example if not using the Vite proxy:
# VITE_DUEL_WS=ws://localhost:3001/duel
#
# Production example only if the duel server is on a different host:
# VITE_DUEL_WS=wss://your-app.onrender.com/duel
